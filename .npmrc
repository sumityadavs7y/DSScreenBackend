# NPM Configuration for A2 Hosting

# Use production mode for faster installs
production=true

# Reduce logging
loglevel=warn

# Don't install devDependencies
# only=production

# Disable package-lock
# package-lock=false

# Timeout for slow connections
fetch-timeout=300000

# Registry (use default npm registry)
registry=https://registry.npmjs.org/

