{
  "name": "digital-signage-backend",
  "version": "1.0.0",
  "description": "Digital Signage - Backend infrastructure by Logical Valley Infotech",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon -r dotenv/config index.js",
    "migrate": "NODE_OPTIONS='--max-old-space-size=2048' npx sequelize-cli db:migrate",
    "migrate:lowmem": "NODE_OPTIONS='--max-old-space-size=1024 --expose-gc' npx sequelize-cli db:migrate",
    "migrate:undo": "npx sequelize-cli db:migrate:undo",
    "migrate:status": "npx sequelize-cli db:migrate:status",
    "migrate:create": "npx sequelize-cli migration:generate --name",
    "migrate:run": "node -e \"require('./utils/migrate').runMigrations().then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\"",
    "migrate:rollback": "node -e \"require('./utils/migrate').rollbackMigration().then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\"",
    "migrate:check": "node -e \"require('./utils/migrate').checkMigrationStatus().then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\"",
    "cleanup:sessions": "node scripts/cleanup-registration-sessions.js",
    "cleanup:sessions:dry-run": "node scripts/cleanup-registration-sessions.js --dry-run"
  },
  "keywords": [
    "backend",
    "express",
    "database",
    "sequelize"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "aws-sdk": "^2.1693.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-session": "^1.18.2",
    "express-validator": "^7.0.1",
    "fluent-ffmpeg": "^2.1.3",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "qrcode": "^1.5.4",
    "sequelize": "^6.37.7",
    "socket.io": "^4.8.3",
    "sqlite3": "^5.1.7",
    "umzug": "^3.8.2",
    "winston": "^3.19.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "sequelize-cli": "^6.6.3"
  }
}
