<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Company - Digital Signage</title>
    <link rel="icon" type="image/png" href="/logo.png">
    <link rel="apple-touch-icon" href="/logo.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 2rem 0;
        }
        .selection-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            max-width: 1200px;
            margin: 0 auto;
        }
        .selection-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 2rem;
            text-align: center;
        }
        .selection-body {
            padding: 2.5rem;
        }
        .col-md-6, .col-lg-4 {
            animation: fadeInUp 0.5s ease forwards;
            opacity: 0;
        }
        .col-md-6:nth-child(1), .col-lg-4:nth-child(1) { animation-delay: 0.1s; }
        .col-md-6:nth-child(2), .col-lg-4:nth-child(2) { animation-delay: 0.2s; }
        .col-md-6:nth-child(3), .col-lg-4:nth-child(3) { animation-delay: 0.3s; }
        .col-md-6:nth-child(4), .col-lg-4:nth-child(4) { animation-delay: 0.4s; }
        .col-md-6:nth-child(5), .col-lg-4:nth-child(5) { animation-delay: 0.5s; }
        .col-md-6:nth-child(6), .col-lg-4:nth-child(6) { animation-delay: 0.6s; }
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .company-card {
            border: 2px solid #e9ecef;
            border-radius: 15px;
            padding: 2rem 1.5rem;
            transition: all 0.3s ease;
            cursor: pointer;
            background: white;
            position: relative;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .company-card:hover {
            border-color: #667eea;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.25);
            transform: translateY(-8px);
            background: linear-gradient(135deg, rgba(102, 126, 234, 0.05) 0%, rgba(118, 75, 162, 0.05) 100%);
        }
        .company-card:active {
            transform: translateY(-4px);
        }
        .company-logo {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            font-weight: bold;
            color: white;
            margin-bottom: 1.5rem;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }
        .company-name {
            font-size: 1.25rem;
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 0.75rem;
            text-align: center;
        }
        .company-role-badge {
            font-size: 0.875rem;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-weight: 500;
            margin-bottom: 1.25rem;
        }
        .select-btn {
            width: 100%;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 10px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.2s;
            box-shadow: 0 4px 10px rgba(102, 126, 234, 0.3);
            margin-top: auto;
        }
        .select-btn:hover {
            background: linear-gradient(135deg, #5568d3 0%, #6a3f8f 100%);
            box-shadow: 0 6px 15px rgba(102, 126, 234, 0.4);
            transform: translateY(-2px);
        }
        .select-btn:active {
            transform: translateY(0);
        }
        .company-card-wrapper {
            height: 100%;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="selection-card">
            <div class="selection-header">
                <img src="/logo.png" alt="Digital Signage Logo" style="height: 60px; margin-bottom: 15px;">
                <h1 class="mb-2">Select Company</h1>
                <p class="mb-0">Welcome <%= user.firstName %>! Choose a company to continue</p>
            </div>
            <div class="selection-body">
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i><%= error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                <% } %>

                <div class="row g-4">
                    <% companies.forEach(company => { %>
                        <div class="col-md-6 col-lg-4">
                            <div class="company-card-wrapper">
                                <div class="company-card">
                                    <div class="company-logo">
                                        <%= company.name.substring(0, 2).toUpperCase() %>
                                    </div>
                                    <h4 class="company-name"><%= company.name %></h4>
                                    <div class="company-role-badge">
                                        <i class="bi bi-person-badge"></i> <%= company.role.charAt(0).toUpperCase() + company.role.slice(1) %>
                                    </div>
                                    <form action="/company-selection" method="POST" style="width: 100%;">
                                        <input type="hidden" name="companyId" value="<%= company.id %>">
                                        <button type="submit" class="select-btn">
                                            <i class="bi bi-arrow-right-circle-fill me-2"></i>Select & Continue
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>

                <hr class="my-4" style="opacity: 0.1;">

                <div class="text-center">
                    <form action="/logout" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-outline-danger btn-lg" style="border-radius: 10px; padding: 0.75rem 2rem;">
                            <i class="bi bi-box-arrow-right me-2"></i>Logout
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
